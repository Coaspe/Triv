export type ModelDetails = {
  id: string;
  name: string;
  height?: string;
  weight?: string;
  size?: string;
  shows?: string[];
  modelingInfo?: string[];
  images?: string[];
};

export type Work = {
  title: string;
  youtubeId: string;
};
